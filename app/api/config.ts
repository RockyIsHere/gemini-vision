const PROMPT = `Think step by step and stay within the context of the image  
- Provide a list of all the following attributes in descending order of the count  in JSON format:
Name, Company, Count`;

module.exports = PROMPT;
